name: pothole_detector_flutter
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  font_awesome_flutter: ^10.1.0
  cupertino_icons: ^1.0.2
  http: ^0.13.3
  geolocator: ^7.7.0
  image_picker: ^0.8.5
  permission_handler: ^9.2.0
  tflite_flutter: ^0.10.3
  camera: ^0.9.4+21
  image: ^4.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/model/
